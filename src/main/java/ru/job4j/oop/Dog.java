package ru.job4j.oop;

public class Dog {
    public static void main(String[] args) {
        Dog polkan = new Dog();  // Первый объект
        Dog sharik = new Dog();   // Второй объект
        Dog zhychka = new Dog();  // Третий объект
    }
}